import { ComingSoon } from "@/components/ComingSoon";

export const metadata = {
  title: "Operação | Vértice",
};

export default function OperacaoPage() {
  return (
    <ComingSoon
      title="Operação"
      description="Acompanhamento de clientes ativos e sucesso do cliente (customer success). Ainda em construção."
    />
  );
}
